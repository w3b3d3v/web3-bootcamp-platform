import { styled } from '@nextui-org/react'

export const Content = styled('div', {
        height: '100vh',
        alignItems: 'center',
        justifyContent: 'center',
        maxW: '800px',
        margin:'auto'
})